import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fr.grithub.app',
  appName: 'Grithub',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
